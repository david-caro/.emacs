;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "/home/cascara/.emacs.d/python/extensions/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 0)
	(left . 1920)
	(width . 235)
	(height . 59)))

